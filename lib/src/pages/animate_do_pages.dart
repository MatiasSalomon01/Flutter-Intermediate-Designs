import 'package:flutter/material.dart';

class AnimateDoPage extends StatelessWidget {
  const AnimateDoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('AnimateDoPage'),
      ),
    );
  }
}
